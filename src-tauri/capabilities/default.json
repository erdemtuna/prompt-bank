{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline core permissions for the single main window: app, event, window, and webview only. No filesystem, dialog, shell, http, updater, image, or path permissions are exposed to the frontend; every private filesystem access goes through the app's own narrow commands, and the folder picker runs in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}

